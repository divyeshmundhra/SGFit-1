/**
 * This class implements the Conversational Nutritional Expert chat-bot feature.
 *
 * @author XYZ
 */
import 'package:flutter/material.dart';

class ChatbotDisplay extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return MaterialApp(

    );
  }
}